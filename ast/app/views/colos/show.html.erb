<p>
  <b>Name:</b>
  <%=h @colo.name %>
</p>

<p>
  <b>Description:</b>
  <%=h @colo.description %>
</p>

<table>
  <tr>
    <th>Name</th>
<!--    <th>Colo</th> -->
  </tr>

<% for asset in @colo.assets %>
  <tr>
    <td><%=h asset.name %></td>
<!--    <td><%=h asset.colo.name %></td> -->
    <td><%= link_to 'Show', asset %></td>
    <td><%= link_to 'Edit', edit_asset_path(asset) %></td>
    <td><%= link_to 'Destroy', asset, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Edit', edit_colo_path(@colo) %> |
<%= link_to 'Back', colos_path %>
