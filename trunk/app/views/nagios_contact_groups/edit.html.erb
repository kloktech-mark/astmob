<h1>Editing nagios_contact_group</h1>

<%= error_messages_for :nagios_contact_group %>

<% form_for(@nagios_contact_group) do |f| %>
  <p>
    <b>Name</b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @nagios_contact_group %> |
<%= link_to 'Back', nagios_contact_groups_path %>
